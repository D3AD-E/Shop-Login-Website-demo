<!DOCTYPE html>

<html>

<head>
    <link rel="stylesheet" type="text/css" href="css/style.css" />
</head>

<body>
    {{>header}}
    <div class="container-login">
        {{#if userInfo}}
        <div class="user-profile-mini">
            <h3>Welcome</h3>
            <h3>{{userInfo.name}}</h3>
            <p>Balance: {{userInfo.coins}} coins</p>
            <p><a href="/user/profile" class="btn btn-outline-primary">My profile</a></p>
            <p><a href="/user/settings" class="btn btn-outline-primary">Settings</a></p>
            <p><a href="/shop/" class="btn btn-outline-primary">Shop</a></p>
            <a href="/user/logout" class="btn btn-outline-secondary">Logout</a>
        </div>
        {{else}}
        <div class="login-form">
            <h1 class="text-center mb-3"><i class="fas fa-sign-in-alt"></i>  Login</h1>
            <form action="/user/login" method="POST">
                <div class="form-group">
                    <input type="email"
                           id="email"
                           name="email"
                           class="form-control"
                           placeholder="Enter Email" />
                </div>
                <div class="form-group">
                    <input type="password"
                           id="password"
                           name="password"
                           class="form-control"
                           placeholder="Enter Password" />
                </div>
                <button type="submit" class="btn">Login</button>
            </form>
            <p class="reg-a">
                No Account? <a href="/user/register">Register</a>
            </p>
        </div>
        {{/if}}

    </div>
</body>

</html>
